Write a program to convert decimal number to binary using recursive function.

Input & Output Format:

Input consists of one integer.

Output consists of a binary value for the given input.

Sample Input & Output:

5


101

#python
def d(n):
    if n==0:
        return 0
    else:
        return (n % 2 + 10 *d(int(n // 2)))
n=int(input())
print(d(n))
 
